Run The Guardian quality checks for the Sentra Assist project:

1. Run `pnpm typecheck` to verify TypeScript strict compliance
2. Run `pnpm lint` to check ESLint rules (zero warnings allowed)
3. Run `pnpm test` to execute Vitest test suite
4. Report results: list any failures with file paths and error messages
5. If any step fails, propose specific fixes for each error
6. Do NOT mark task as done until all three checks pass (GREEN)
